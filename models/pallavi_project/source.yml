version: 2
sources:
    - name: datafeed_shared_schema
      database: "{{('dbt_new')}}"
      schema: source_new
      tables:
        - name: stg_customerdata
        - name: stg_payments
        - name: raw_orders
 
